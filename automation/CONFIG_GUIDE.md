# 📖 Повний гід по налаштуваннях config.json

## 🎯 Загальна структура

```json
{
  "browser": { ... },      // Налаштування браузера
  "storage": { ... },      // Налаштування storage
  "ui": { ... },           // Налаштування інтерфейсу
  "timeouts": { ... },     // Таймаути
  "retries": { ... },      // Повтори
  "notifications": { ... } // Нотифікації
}
```

---

## 🌐 Browser - Налаштування браузера

### `executablePath`
**Що це:** Шлях до виконуваного файлу Brave браузера

**Дефолт:**
```json
"/Applications/Brave Browser.app/Contents/MacOS/Brave Browser"
```

**Як змінити:** Якщо Brave встановлений в іншому місці
```json
"/Users/yourusername/Applications/Brave Browser.app/Contents/MacOS/Brave Browser"
```

---

### `debugPort`
**Що це:** Порт для Chrome DevTools Protocol (CDP)

**Дефолт:** `9222`

**Коли змінювати:** Якщо порт 9222 зайнятий іншим додатком

---

### `userDataDir`
**Що це:** Папка з профілем браузера для автоматизації

**Дефолт:**
```json
"/Users/mykhailo.vynnyk/Library/Application Support/BravePlaywright"
```

**ВАЖЛИВО:** Змініть `mykhailo.vynnyk` на своє ім'я користувача!

---

### `autoCloseTab` ⭐ НОВА ОПЦІЯ
**Що це:** Автоматично закривати вкладку після завантаження/скасування

**Дефолт:** `true`

**Варіанти:**
- `true` - Закривати вкладку автоматично
- `false` - Залишати вкладку відкритою

**Приклад:**
```json
"autoCloseTab": false  // Вкладка залишиться відкритою
```

---

### `closeDelaySuccess` ⭐ НОВА ОПЦІЯ
**Що це:** Затримка (в мілісекундах) перед закриттям вкладки після успішного завантаження

**Дефолт:** `0` (закривається одразу)

**Варіанти:**
```json
"closeDelaySuccess": 0      // Без затримки
"closeDelaySuccess": 2000   // 2 секунди
"closeDelaySuccess": 5000   // 5 секунд
```

**Коли використовувати:** Якщо хочеш побачити результат перед закриттям

---

### `closeDelayCancel` ⭐ НОВА ОПЦІЯ
**Що це:** Затримка (в мілісекундах) перед закриттям вкладки після скасування

**Дефолт:** `3000` (3 секунди)

**Варіанти:**
```json
"closeDelayCancel": 0      // Одразу закривати
"closeDelayCancel": 3000   // 3 секунди (дефолт)
"closeDelayCancel": 5000   // 5 секунд
```

**Коли використовувати:** Регулюй скільки часу бачиш повідомлення "Скасовано"

---

## 🗄️ Storage - Налаштування сховища

### `baseUrl`
**Що це:** URL для доступу до storage (через інтерфейс)
```json
"baseUrl": "https://storage.epcnetwork.dev"
```

### `publicUrl`
**Що це:** Публічний URL для доступу до файлів
```json
"publicUrl": "https://storage.5th-elementagency.com"
```

### `basePath`
**Що це:** Базовий шлях для всіх файлів
```json
"basePath": "Promo"
```

---

## 🎨 UI - Інтерфейс

### `formServerPort`
**Що це:** Порт для HTTP сервера форми

**Дефолт:** `3838`

**Коли змінювати:** Якщо порт зайнятий
```json
"formServerPort": 3939
```

### `interactiveMode`
**Що це:** Показувати форму підтвердження (наразі не використовується)

---

## ⏱️ Timeouts - Таймаути

### `browserStart`
**Що це:** Час очікування запуску браузера (мс)

**Дефолт:** `1500` (1.5 секунди)

**Збільш якщо:** Браузер повільно запускається
```json
"browserStart": 3000  // 3 секунди
```

### `pageLoad`
**Що це:** Максимальний час завантаження сторінки (мс)

**Дефолт:** `10000` (10 секунд)

### `elementWait`
**Що це:** Час очікування елементів на сторінці (мс)

**Дефолт:** `5000` (5 секунд)

### `uploadRetry`
**Що це:** Затримка перед повторною спробою завантаження (мс)

**Дефолт:** `2000` (2 секунди)

### `interfaceCheck`
**Що це:** Інтервал перевірки готовності інтерфейсу (мс)

**Дефолт:** `200` (0.2 секунди)

### `interfaceMaxChecks`
**Що це:** Максимальна кількість перевірок готовності інтерфейсу

**Дефолт:** `10` (разом 2 секунди: 10 × 200мс)

---

## 🔁 Retries - Повтори

### `uploadAttempts`
**Що це:** Кількість спроб завантаження файлу

**Дефолт:** `2` (перша спроба + 1 повтор)

**Варіанти:**
```json
"uploadAttempts": 1   // Без повторів
"uploadAttempts": 3   // Дві повторні спроби
```

---

## 🔔 Notifications - Нотифікації

### `enabled`
**Що це:** Показувати системні нотифікації та відтворювати звуки

**Дефолт:** `true`

**Вимкнути:**
```json
"enabled": false
```

### `sounds`
**Що це:** Шляхи до звукових файлів macOS

```json
"sounds": {
  "success": "/System/Library/Sounds/Blow.aiff",
  "error": "/System/Library/Sounds/Basso.aiff",
  "warning": "/System/Library/Sounds/Sosumi.aiff"
}
```

**Інші звуки macOS:**
- `Ping.aiff` - м'який звук
- `Pop.aiff` - короткий клік
- `Glass.aiff` - скляний звук
- `Hero.aiff` - героїчний
- `Submarine.aiff` - підводний човен

---

## 🎯 Швидкі сценарії налаштування

### Сценарій 1: Залишати вкладку відкритою
```json
{
  "browser": {
    "autoCloseTab": false
  }
}
```

### Сценарій 2: Показувати результат 5 секунд перед закриттям
```json
{
  "browser": {
    "autoCloseTab": true,
    "closeDelaySuccess": 5000,
    "closeDelayCancel": 5000
  }
}
```

### Сценарій 3: Швидке закриття без затримок
```json
{
  "browser": {
    "autoCloseTab": true,
    "closeDelaySuccess": 0,
    "closeDelayCancel": 0
  }
}
```

### Сценарій 4: Вимкнути всі нотифікації
```json
{
  "notifications": {
    "enabled": false
  }
}
```

### Сценарій 5: Повільний комп'ютер
```json
{
  "timeouts": {
    "browserStart": 3000,
    "pageLoad": 20000,
    "elementWait": 10000
  }
}
```

### Сценарій 6: Більше спроб завантаження
```json
{
  "retries": {
    "uploadAttempts": 5
  }
}
```

---

## ⚠️ Важливі нотатки

1. **Після змін config.json** - перезапустіть скрипт
2. **Невалідний JSON** - скрипт не запуститься (перевіряй коми та лапки!)
3. **Шляхи в Windows** - використовуй подвійні слеші: `C:\\Program Files\\...`
4. **Порти** - переконайся що порти вільні (9222, 3838)

---

## 🔧 Налагодження

### Перевірити чи config.json валідний:
```bash
cat config.json | python -m json.tool
```

### Перевірити чи порт вільний:
```bash
lsof -i :9222
lsof -i :3838
```

### Знайти звуки macOS:
```bash
ls /System/Library/Sounds/
```

---

**Готово! Налаштовуй під себе! 🚀**

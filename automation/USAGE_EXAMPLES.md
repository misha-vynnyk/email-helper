# 📖 Приклади використання

## 📝 Основний режим (з формою підтвердження)

### Приклад 1: Простий запуск
```bash
node scripts/upload-playwright-brave.js ~/Downloads/screenshot.png
```

**Що відбудеться:**
1. Відкриється форма підтвердження в Brave
2. Ви побачите: назву файлу, розмір, повний шлях
3. Якщо в буфері є текст - він автоматично підставиться в поле "Назва папки"
4. Ви вводите/підтверджуєте `ABCD123` та обираєте `Finance`
5. Натискаєте "✓ Завантажити"
6. Скрипт автоматично завантажує файл на storage
7. Шлях копіюється в буфер обміну + показується нотифікація

---

### Приклад 2: Batch завантаження через wrapper
```bash
./save-in-finance.sh ~/Downloads/img1.jpg ~/Downloads/img2.png ~/Downloads/doc.pdf
```

**Що відбудеться:**
1. Для кожного файлу відкриється форма
2. Категорія "Finance" вже обрана (з назви скрипта)
3. Вводите назву папки для кожного файлу
4. Всі файли завантажуються послідовно

---

### Приклад 3: З попереднім копіюванням
```bash
# Крок 1: Скопіюйте назву папки
echo "ABCD123" | pbcopy

# Крок 2: Запустіть скрипт
node scripts/upload-playwright-brave.js ~/file.jpg

# Форма відкриється з вже заповненою назвою папки!
```

---

## ⚡ Режим без підтвердження (для автоматизації)

### Приклад 4: Пропуск форми з --no-confirm
```bash
# Скопіюйте назву папки
echo "Finance-789" | pbcopy

# Завантажте файл з прапорцем --no-confirm
node scripts/upload-playwright-brave.js ~/file.jpg finance --no-confirm

# Або скорочено з -y
node scripts/upload-playwright-brave.js ~/file.jpg finance -y
```

**Результат:** Файл завантажиться без відкриття форми (дані з буфера)

**Важливо:** Категорія обов'язкова в режимі --no-confirm!

---

## 🔄 Реальні сценарії використання

### Сценарій 1: Завантаження скріншотів з Finance проекту
```bash
# Є 5 скріншотів для клієнта DELTA456
cd ~/Desktop

# Скопіюйте код клієнта
echo "DELTA456" | pbcopy

# Завантажте всі файли
./save-in-finance.sh screenshot-*.png
```

---

### Сценарій 2: Швидке завантаження одного файлу
```bash
# Ви працюєте з клієнтом GAMMA789
# Назва вже в буфері після копіювання з документа

# Просто перетягніть файл на скрипт в терміналі або:
node scripts/upload-playwright-brave.js /path/to/file.jpg
```

Форма відкриється з автозаповненням!

---

### Сценарій 3: Автоматизація через LaunchBar/Alfred
Створіть shortcut:
```bash
#!/bin/bash
# LaunchBar action: Upload to Storage

node "$HOME/Documents/projects/automation/scripts/upload-playwright-brave.js" "$1"
```

Тепер можна:
1. Обрати файл в Finder
2. LaunchBar → "Upload to Storage"
3. Заповнити форму
4. Готово!

---

### Сценарій 4: Інтеграція з Automator (macOS)
1. Відкрийте Automator
2. Створіть "Quick Action"
3. Додайте "Run Shell Script"
4. Вставте:
```bash
for f in "$@"; do
  /usr/local/bin/node "$HOME/Documents/projects/automation/scripts/upload-playwright-brave.js" "$f"
done
```
5. Збережіть як "Upload to EPC Storage"

Тепер в Finder:
- Правий клік на файл
- Quick Actions → Upload to EPC Storage
- Форма відкриється автоматично!

---

## 🎯 Поради

### Порада 1: Групове завантаження з однією папкою
Якщо всі файли йдуть в одну папку:
```bash
# Скопіюйте назву один раз
echo "ALPHA123" | pbcopy

# Завантажте всі файли
./save-in-finance.sh file1.jpg file2.pdf file3.png

# Назва папки автозаповниться для кожного файлу
# Просто натискайте Enter!
```

---

### Порада 2: Скасування
В будь-який момент можна:
- Натиснути "Скасувати" в формі
- Натиснути `Esc`
- Закрити вікно браузера
- `Ctrl+C` в терміналі

---

### Порада 3: Перевірка результату
Після завантаження шлях до файлу в буфері:
```bash
# Вставте в браузер
# Cmd+V покаже: files/Promo/finance/abcd/lift-123/file.jpg

# Або відразу відкрийте:
pbpaste | xargs -I {} open "https://storage.5th-elementagency.com/{}"
```

---

## ❓ FAQ

**Q: Чи можу я змінити категорію в формі?**
A: Так! Навіть якщо категорія визначена з шляху файлу, ви можете змінити її в формі.

**Q: Що якщо я введу неправильну назву папки?**
A: Форма перевірить формат перед відправкою. Потрібні і літери, і цифри.

**Q: Чи можна завантажувати без форми взагалі?**
A: Так, використовуйте прапорець --no-confirm:
```bash
# Спочатку скопіюйте назву папки
echo "ABCD123" | pbcopy
# Потім запустіть
node scripts/upload-playwright-brave.js file.jpg finance --no-confirm
```

**Q: Форма не закривається після завантаження**
A: Це нормально, можна просто закрити вкладку. Завантаження вже відбулось.

---

## 🚀 Швідкий старт для нових користувачів

```bash
# 1. Є файл для завантаження
FILE="~/Downloads/report.pdf"

# 2. (Опціонально) Скопіюйте назву папки для автозаповнення
echo "ABCD123" | pbcopy

# 3. Запустіть скрипт
node scripts/upload-playwright-brave.js "$FILE"

# 4. У формі що відкрилась:
#    - Перевірте інформацію про файл
#    - Підтвердіть/змініть назву папки (ABCD123 вже заповнена!)
#    - Оберіть: Finance або Health
#    - Натисніть: ✓ Завантажити

# 5. Скрипт автоматично завантажує файл
# 6. Готово! Шлях в буфері обміну + нотифікація
```

---

**Успіхів! 🎉**

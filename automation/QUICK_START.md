# 🚀 Швидкий старт

## 📋 За 5 хвилин до першого завантаження

### 1️⃣ Встановлення (одноразово)

```bash
# Перейди в папку проекту
cd ~/Documents/projects/automation

# Встанови залежності
npm install
```

### 2️⃣ Налаштування (одноразово)

Відкрий `config.json` і зміни:

```json
{
  "browser": {
    "userDataDir": "/Users/ТВОЄ_ІМЯ/Library/Application Support/BravePlaywright"
  }
}
```

Замість `ТВОЄ_ІМЯ` впиши своє ім'я користувача macOS.

### 3️⃣ Використання (щоразу)

**Варіант А: Через bash скрипт (рекомендовано)**

```bash
# Для Finance
./save-in-finance.sh ~/Downloads/file.jpg

# Для Health
./save-in-health.sh ~/Downloads/file.jpg
```

**Варіант Б: Напряму через Node**

```bash
node scripts/upload-playwright-brave.js ~/Downloads/file.jpg
```

### 4️⃣ Що відбудеться?

1. 🚀 Відкриється Brave браузер
2. 📝 Побачиш форму з інформацією про файл
3. 📋 Введи назву папки (напр. `ABCD123`)
4. 🏷️ Обери категорію (Finance/Health)
5. ✓ Натисни "Завантажити"
6. ⬆️ Файл автоматично завантажиться
7. 📋 Шлях скопіюється в буфер
8. ✅ Вкладка закриється, скрипт завершиться

---

## ⚙️ Швидкі налаштування

### Залишати вкладку відкритою

```json
// config.json
{
  "browser": {
    "autoCloseTab": false
  }
}
```

### Показувати результат 5 секунд

```json
{
  "browser": {
    "closeDelaySuccess": 5000
  }
}
```

### Вимкнути звуки

```json
{
  "notifications": {
    "enabled": false
  }
}
```

---

## 🎯 Приклади реального використання

### Завантажити один файл

```bash
./save-in-finance.sh ~/Desktop/screenshot.png
```

### Завантажити кілька файлів

```bash
./save-in-finance.sh ~/Desktop/*.jpg
```

### З передкопійованою назвою папки

```bash
# Спочатку скопіюй назву папки
echo "DELTA456" | pbcopy

# Потім запусти (назва автозаповниться!)
./save-in-finance.sh ~/file.jpg
```

---

## ❓ Питання та відповіді

**Q: Як дізнатись своє ім'я користувача?**
```bash
echo $USER
```

**Q: Браузер не запускається**
A: Перевір шлях до Brave в `config.json` → `executablePath`

**Q: Порт зайнятий (3838 або 9222)**
A: Зміни в `config.json`:
```json
{
  "browser": { "debugPort": 9223 },
  "ui": { "formServerPort": 3839 }
}
```

**Q: Як пропустити форму для автоматизації?**
```bash
node scripts/upload-playwright-brave.js file.jpg finance --no-confirm
```

---

## 📖 Детальна документація

- [README.md](README.md) - Повна інструкція
- [CONFIG_GUIDE.md](CONFIG_GUIDE.md) - Всі налаштування
- [USAGE_EXAMPLES.md](USAGE_EXAMPLES.md) - Приклади використання
- [CHANGELOG.md](CHANGELOG.md) - Історія змін

---

## 🆘 Потрібна допомога?

1. Перевір чи файли існують
2. Перевір config.json (вірний JSON?)
3. Запусти з `-v` для детальних логів
4. Перегляньте документацію вище

**Готово! Завантажуй файли! 🎉**

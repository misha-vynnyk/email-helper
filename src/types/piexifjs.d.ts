declare module "piexifjs" {
  // piexifjs ships no types; `any` is the accepted shape for an untyped third-party module.
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const piexif: any;
  export default piexif;
}


import type { SimpleTokensOverride } from "../config/tokens";

/** Default profile — no overrides; base `tokens` in `config/tokens.ts` already holds these values. */
export const profile: SimpleTokensOverride = {};

/**
 * Stage 2 image placeholder. Deliberately local to figmaImport and not provider-aware —
 * which storage provider a finished Figma-import template uploads through is an open
 * question left for a later stage (see Stage 2 plan, renderImage note).
 */
export const PLACEHOLDER_IMAGE_SRC = "https://storage.5th-elementagency.com/placeholder.png";
